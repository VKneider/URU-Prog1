#include <iostream>
using namespace std;

int main() {
    /*
    Enunciado del ejercicio:
    Escribe un programa que utilice funciones para realizar las siguientes operaciones matemáticas:
    
    1. Crea una función llamada `calcularFactorial` que reciba un número entero positivo y devuelva su factorial.
    2. Crea una función llamada `esPrimo` que reciba un número entero positivo y devuelva `true` si el número es primo, o `false` en caso contrario.
    3. En el programa principal (`main`), solicita al usuario un número entero positivo.
    4. Usa las funciones para calcular el factorial del número y determinar si es primo.
    5. Muestra los resultados en la consola.

    Ejemplo de ejecución:
    Ingresa un número entero positivo: 5
    El factorial de 5 es: 120
    ¿Es primo? Sí
    */

    return 0;
}