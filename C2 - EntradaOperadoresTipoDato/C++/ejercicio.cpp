#include <iostream>

using namespace std;

int main() {
    /*
    Enunciado del ejercicio:
    Escribe un programa que resuelva la siguiente ecuación matemática:
    
        z = (x^2 + y^2) / (2 * x * y)

    Donde:
    - x e y son números enteros que el usuario debe ingresar.
    - Asegúrate de que el denominador no sea cero antes de realizar la operación.
    - Muestra el resultado de z en la consola.

    Si lo deseas y te motivasa investigar:  incluye un mensaje que indique si el resultado de z es mayor, menor o igual a 1.
    */

    return 0;
}