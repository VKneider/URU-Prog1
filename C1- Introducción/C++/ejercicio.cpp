#include <iostream>
using namespace std;

int main() {
    /*
    Enunciado del ejercicio:
    Escribe un programa que utilice namespaces para definir variables con el mismo nombre pero con valores diferentes.
    
    Reglas:
    1. Crea un namespace llamado `Espacio1` que contenga una variable llamada `valor` con un valor entero.
    2. Crea otro namespace llamado `Espacio2` que también contenga una variable llamada `valor` con un valor entero diferente.
    3. En el programa principal (`main`), imprime el valor de la variable `valor` de cada namespace.
    4. No uses `using namespace` para evitar conflictos y accede a las variables utilizando el operador de resolución de ámbito (`::`).

    Ejemplo de ejecución:
    Valor en Espacio1: 10
    Valor en Espacio2: 20
    */

    return 0;
}