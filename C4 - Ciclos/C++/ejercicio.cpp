#include <iostream>

using namespace std;

int main() {
    /*
    Enunciado del ejercicio:
    Escribe un programa que solicite al usuario ingresar un número entero positivo.
    Luego, utiliza un ciclo `while` para calcular y mostrar la suma de todos los números
    desde 1 hasta el número ingresado.

    Reglas:
    - Si el usuario ingresa un número negativo o cero, muestra un mensaje de error
      y solicita nuevamente un número válido.
    - Una vez que se ingrese un número válido, calcula la suma y muestra el resultado.

    Ejemplo de ejecución:
    Ingresa un número entero positivo: -5
    Número inválido. Por favor, ingresa un número entero positivo: 5
    La suma de los números de 1 a 5 es: 15
    */

    // NO UTILIZAR CONDICIONALES IF/ELSE, SINO EXCLUSIVAMENTE CICLOS.

    return 0;
}